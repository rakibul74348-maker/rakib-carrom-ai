name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Create Dummy Google Services and Keystore
      run: |
        keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
        mkdir -p app
        cp debug.keystore app/debug.keystore
        mkdir -p ~/.android
        cp debug.keystore ~/.android/debug.keystore
        echo '{"project_info":{"project_number":"123456789","project_id":"rakib-carrom-ai","storage_bucket":"rakib.appspot.com"},"client":[{"client_info":{"mobilesdk_app_id":"1:123456789:android:123456789","android_client_info":{"package_name":"com.aistudio.carromaimpro.rkba"}},"oauth_client":[],"api_key":[{"current_key":"AIzaSyFakeKeyForBuildValidationOnly"}],"services":{}}],"configuration_version":"1"}' > app/google-services.json

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    - name: Build with Gradle
      run: gradle assembleDebug --no-daemon -x test

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: rakib-carrom-ai-app
        path: app/build/outputs/apk/debug/*.apk
        
